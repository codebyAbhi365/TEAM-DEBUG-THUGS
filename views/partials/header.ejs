<header class="common-header">
    <span class="header-text">Hi, <%= user ? user.Name : "Guest" %></span>
    <a style="position: relative;right:20px" href="/home/profile" class="nav-item">
        <span class="nav-icon">&#128100;</span> <!-- Profile icon -->
        <span>Profile </span>
    </a>
</header>

<style>
    .nav-item {
        display: flex;
        flex-direction: column;
        align-items: center;
        font-size: 14px;
    }

    .nav-icon {
        font-size: 24px;
    }
    a{
        text-decoration: none;
    }
    .common-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        position: relative;
        right:18px;
        padding: 10px;
        background: white;
        box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1);
        width: 103%;
        margin: 0 auto;
        border-radius: 12px 12px 0 0;
    }

    .header-text {
        font-size: 18px;
        font-weight: bold;
    }

    .bell-icon {
        font-size: 24px;
        cursor: pointer;
    }

    @media (max-width: 1200px) {
        .common-header {
            padding: 20px;
            max-width: 90vw;
        }

        .header-text {
            font-size: 24px;
        }

        .bell-icon {
            font-size: 36px;
        }
    }
</style>