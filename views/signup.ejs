<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Auth Pages</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
           
            font-family: Arial, sans-serif;
            overflow: hidden;
        }
        .container {
            width: 250px;
            height: 500px;
            padding: 20px;
            background: white;
            border-radius: 40px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
            text-align: center;
            position: absolute;
            display: flex;
            flex-direction: column;
            justify-content: center;
            background-image: url(/images/Sky\ Blue\ Simple\ Minimalist\ Illustration\ Do\ More\ Trash\ Bash\ Campaign\ Your\ Story.png); 
            background-size: cover;
            background-position: center;
        }
        h2 {
            color: #556B2F;
            font-size: 22px;
            margin-bottom: 10px;
        }
        .input-container {
            position: relative;
            width: 90%;
            margin: 10px auto;
        }
        input {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 20px;
            outline: none;
        }
        input:focus {
            border-color: #556B2F;
        }
        .toggle-password {
            position: absolute;
            right: 10px;
            top: 50%;
            cursor: pointer;
            color: #666;
        }
        button {
            width: 100%;
            padding: 10px;
            background: #556B2F;
            color: white;
            border: none;
            border-radius: 20px;
            cursor: pointer;
            font-weight: bold;
        }
        button:hover {
            background: #6B8E23;
            transform: translateY(-2px);
            box-shadow: 0 5px 10px rgba(0, 0, 0, 0.3);
        }
        p {
            margin-top: 10px;
            color: #556B2F;
            cursor: pointer;
            font-weight: bold;
        }
        
        .signup-link {
            color: #007bff;
            text-decoration: none;
            font-weight: bold;
        }
    
        .signup-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    
    <div class="container hidden" id="signupPage">
        <!-- <form action="/signup" method="post"> -->
        <h2>Sign Up</h2>
        <div class="input-container">
            <input type="text" placeholder="Full Name" name="Name">
        </div>
        <div class="input-container" >
            <input type="email" placeholder="Email" name="Email">
        </div>
        <div class="input-container">
            <input type="password" id="signupPassword" placeholder="Password" name="Pass">
            <span class="toggle-password" onclick="togglePassword('signupPassword')"></span>
        </div>
        <div class="input-container">
            <input type="tel" placeholder="Mobile Number" name="PhnNo">
        </div>
        <div class="input-container">
            <input type="number" placeholder="Age">
        </div>
        <button> <a href="/home/workhome" target="_blank" rel="noopener noreferrer"></a>  SignUp</button>
        <br><br><br>
        <a class="signup-link" href="/login">already have account? login</a>
        

<a class="signup-link" href="/home/workhome">Worker Profile</a>

    </div>
    
    <script>
        function togglePassword(id = "password") {
            var passwordField = document.getElementById(id);
            if (passwordField.type === "password") {
                passwordField.type = "text";
            } else {
                passwordField.type = "password";
            }
        }
    </script>
</body>
</html>